extends layout/main.pug
include mixins/videoBlock

block content
  .videos
    each item in vides
      +videoBlock({
          id:item.id,
          title:item.title,
          views:item.views,
          videoFile:item.videoFile
        })
