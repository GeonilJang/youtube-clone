extends layout/main.pug
include mixins/videoBlock


block content
  .videos
    each video in vides
      +videoBlock({
          title:video.title,
          views:video.views,
          videoFile:video.videoFile
        })
